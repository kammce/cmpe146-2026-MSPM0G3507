#pragma once

#include <cstdint>

// This is the public interface for the GPIO driver you are writing for
// Lab 1. Implement every function in gpio.cpp using direct register
// access — no `ti/driverlib/dl_gpio.h`, no `DL_GPIO_*` calls, and no
// SysConfig-generated GPIO init code. See README.md for where to find the
// register map you need (MSPM0G350x TRM: GPIO and IOMUX chapters).

namespace gpio {

// To be added

}  // namespace gpio
